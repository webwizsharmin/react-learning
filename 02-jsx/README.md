# JSX

JSX is look like HTML, we use JSX in react compnent so that we can add functionality or add JS very easy to the jsx mark.

- it is little bit stricter.
- when we have to add JS in Our project we can just simply use curly braces.
